/** BullMQ queue names (AGENTS.md §12). */
export const QUEUES = {
  EMAILS: 'emails',
  PAYMENTS: 'payments',
  STREAMING: 'streaming',
  NOTIFICATIONS: 'notifications',
  CLEANUP: 'cleanup',
} as const;
