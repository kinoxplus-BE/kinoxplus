import { Module } from '@nestjs/common';

/**
 * [POST-MVP] analytics event ingestion + dashboard (AGENTS.md §15).
 * Structure exists so imports don't churn later — do not build in MVP.
 */
@Module({})
export class AnalyticsModule {}
